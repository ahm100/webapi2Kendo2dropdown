@model MyEshop.Models.Product_Groups

@{
    ViewBag.Title = "حذف " + Server.HtmlDecode(Html.DisplayNameFor(model => model).ToString());
}

<h2>@ViewBag.Title</h2>

<h3>آیا از حذف @Model.GroupTitle این مورد اطمینان دارید؟</h3>
<div style="overflow: hidden">    
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.GroupTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GroupTitle)
        </dd>

    </dl>

  @using (Ajax.BeginForm("Delete", "Product_Groups", FormMethod.Post, new AjaxOptions()
{
    UpdateTargetId = "showlist",
    OnSuccess = "Success"
}))
  {
        @Html.AntiForgeryToken()

       <div class="form-group" id="buttons">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="تایید حذف" class="btn btn-primary" />
				@Html.ActionLink("بازگشت به فهرست", "Index")
            </div>
        </div>
    }
</div>
